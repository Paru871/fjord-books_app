<h1><%= t('users.views.users_list') %></h1>
 
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, user %>
    </li>
  <% end %>
</ul>
<%= paginate @users %>

<%= link_to t('views.common.books_list'), books_path %>
