<h1><%= Comment.model_name.human %></h1>
<% if commentable.comments.any? %>
<table>
  <thead>
    <tr>
      <th><%= Comment.human_attribute_name(:user) %></th>
      <th><%= Comment.human_attribute_name(:created_at) %></th>
      <th><%= Comment.human_attribute_name(:body) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% commentable.comments.order(created_at: :desc).includes(:user).each do |comment| %>
      <tr>
        <td>
          <% if comment.user.name.empty? %>
            <%= link_to comment.user.email,comment.user %>
          <% else %>
            <%= link_to comment.user.name,comment.user %>
          <% end %>
        </td>
        <td><%= l(comment.created_at, format: :long) %>
        <td><%= comment.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
  <p><%= t('views.common.no_data', name: Comment.model_name.human) %></p>
<% end %>
